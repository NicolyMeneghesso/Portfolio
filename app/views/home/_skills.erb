<div id="skills">

    <!-- Título no topo -->
    <div class="col-12">
        <h1 class="title-skills text-center mt-5 mb-2 slide-in-left">Linguagens e Tecnologias</h1>
    </div>
        
    <div class="row row-skills">

        <!-- Coluna para o Monitor -->
        <div class="col-md-3 monitor-skills">
            <div class="position-relative">

                <%= render partial: 'home/skills_monitor' %>
                <div class="card-simbol">
                    &#60; &#8260; &#62;
                </div>
            </div>
        </div>
        
        <!-- Coluna para os Cards -->
        <div class="col mt-4 d-flex flex-wrap justify-content-center align-itens-start">    
            <div class="row row-skills slide-in-left">
                <% @skills.each do |skill| %>
                    <%= render partial: 'home/skills_card', locals: { skill: skill } %>
                <% end %>
            </div>
        </div>
    </div>
</div>
