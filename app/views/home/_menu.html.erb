<nav class="navbar navbar-expand-lg navbar-dark fixed-top">
    <div class="container-fluid">
        <div class="navbar-brand">
            <div class="nav-text-2 slide-in-left" id="nav-text-2">
                <% text = "<n.meneghesso />" %>
                <% text.each_char do |char| %>
                <p><%= char %></p>
                <% end %>
            </div>
        </div>

        <span id="moon-icon">
            &#9790; 
        </span>

        <button class="navbar-toggler slide-in-left" type="button" data-bs-toggle="collapse" 
            data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse justify-content-center" id="navbarSupportedContent">
            <ul class="navbar-nav mb-2 mb-lg-0 align-items-center">
                <li class="nav-item">
                    <div class="nav-text slide-in-left">
                        <% text = "<n.meneghesso />" %>
                        <% text.each_char do |char| %>
                        <p><%= char %></p>
                        <% end %>
                    </div>
                </li>
                <li class="nav-item slide-in-left">
                    <a class="nav-link" aria-current="page" href="#home">Home</a>
                </li>
                <li class="nav-item slide-in-left">
                    <a class="nav-link" href="#about">Sobre</a>
                </li>
                <li class="nav-item slide-in-left">
                    <a class="nav-link" href="#skills">Habilidades</a>
                </li>
                <li class="nav-item slide-in-left">
                    <a class="nav-link" href="#projects">Projetos</a>
                </li>
            </ul>
            <form class="d-flex align-items-center slide-in-left" style="width: 10%; padding-left: 1%" role="search">
                <label class="custom-toggle btn-outline-success me-2" id="day">
                    <input type="checkbox">
                    <div class="slider"></div>
                </label>
            </form>
        </div> 
    </div>
</nav>

